export interface SimulationInterest {
  id: string;
  user_id: string;
  startup_id: string;
  email: string;
  created_at: string;
}

export interface SimulationInterestStatus {
  interested: boolean;
}

